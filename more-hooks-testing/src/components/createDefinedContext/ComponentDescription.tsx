const ComponentDescription = () => {
  return (
    <>
      <h2 id="createDefinedContext">
        <code>createDefinedContext</code>
      </h2>
      <p>
        An implementation on top of React Context. It ensures the existance of
        your ContextProvider and its value. The content of your context can
        therefore never be undefined.
      </p>
    </>
  );
};

export default ComponentDescription;
