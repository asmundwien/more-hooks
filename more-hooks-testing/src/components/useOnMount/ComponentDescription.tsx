const ComponentDescription = () => {
  return (
    <>
      <h2 id="useOnMount">
        <code>useOnMount</code>
      </h2>
      <p>
        This tiny hook will run only during your components first render lifecycle. Remeber <code>componentDidMount()</code>?.
      </p>
    </>
  );
};

export default ComponentDescription;
